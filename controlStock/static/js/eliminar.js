// Sin uso:
function confirmarEliminacion() { 
    // Cuadro de dialogo
    var confirmacion = confirm("¿Está segur@ de eliminarlo?");  
    // Si el usuario confirma continua al enlace
    if (confirmacion) {
        return true;
    } else {
        return false;
    }
  }
// ---